# 构建与发布指南

本指南主要说明如何配置 Android、Windows、macOS 应用的签名和发布，以及解决 GitHub
Actions 构建过程中常见的权限问题。

## 一、配置 Android 发布签名

为了自动构建并发布 Android 正式版（Release
APK），你需要生成一个签名密钥，并将其配置到 GitHub Secrets 中。

### 1. 生成密钥库 (Keystore)

在终端运行以下命令（需要安装 JDK 或 Android SDK）：

```bash
keytool -genkey -v -keystore release.keystore -alias seedclaw -keyalg RSA -keysize 2048 -validity 10000
```

_(注意：请妥善保管好 `release.keystore` 文件，如果丢失将无法更新已发布的应用！)_

输入密码并填写相关信息（如姓名、单位等）。**请务必记住设置的 Keystore 密码和 Key
密码**。

### 2. 将密钥库编码为 Base64

GitHub Secrets 无法直接存储二进制文件，因此需要将生成的 `.keystore` 文件转换为
Base64 字符串。

#### Windows (PowerShell)

```powershell
[Convert]::ToBase64String([IO.File]::ReadAllBytes("release.keystore")) | Set-Clipboard
```

_(上述命令会将 Base64 字符串直接复制到你的剪贴板)_

#### Mac / Linux

```bash
base64 -i release.keystore | pbcopy
```

### 3. 配置 GitHub 仓库 Secrets

进入你的 GitHub 仓库 -> **Settings** -> **Secrets and variables** -> **Actions**
-> **New repository secret**。

添加以下 4 个 Secrets：

| Secret 名称                 | 值                                  | 说明                     |
| :-------------------------- | :---------------------------------- | :----------------------- |
| `ANDROID_KEYSTORE_BASE64`   | _(粘贴第 2 步生成的 Base64 字符串)_ | 编码后的密钥库文件内容。 |
| `ANDROID_KEYSTORE_PASSWORD` | _(你的密钥库密码)_                  | 第 1 步设置的密码。      |
| `ANDROID_KEY_ALIAS`         | `seedclaw`                          | 第 1 步设置的别名。      |
| `ANDROID_KEY_PASSWORD`      | _(你的密钥密码)_                    | 通常与密钥库密码相同。   |

## 二、配置 Windows/macOS/Linux 桌面端签名

### 1. 概念区分：代码签名 vs 更新签名

| 特性         | 代码签名 (Code Signing)                           | 更新签名 (Updater Signature)              |
| :----------- | :------------------------------------------------ | :---------------------------------------- |
| **验证者**   | 操作系统 (Windows SMART SCREEN, macOS Gatekeeper) | 你的 Tauri 应用 (Tauri Updater)           |
| **如果不做** | 安装时弹窗警告 (Windows) 或无法运行 (macOS)       | 无法使用软件内的自动更新功能              |
| **成本**     | **昂贵** (Windows 几百美元/年, macOS $99/年)      | **免费** (本地生成密钥对即可)             |
| **私钥来源** | 向证书颁发机构 (CA) 购买                          | 使用 `tauri signer generate` 生成         |
| **文件形式** | 嵌入在 `.exe` / `.app` 文件内部                   | 独立的 `.sig` 文件 (如 `app.msi.zip.sig`) |

**结论**：哪怕你不买代码签名证书（忍受安装时的警告），你也**强烈建议配置更新签名**，否则你的应用将来无法平滑地自动升级。

---

### 2. 配置 Windows 签名 (Code Signing - 需购买证书)

如果你购买了代码签名证书 (通常是 `.pfx` 或 `.p12` 格式)：

1. **准备证书**：将证书文件 Base64 编码，存入 GitHub Secrets，比如命名为
   `WINDOWS_CERTIFICATE_BASE64`，密码存为 `WINDOWS_CERTIFICATE_PASSWORD`。
2. **修改 `release.yml`**：在 Windows 构建步骤中添加以下逻辑。

```yaml
# 1. 解码证书
- name: Decode certificate
  if: matrix.platform == 'windows-latest'
  run: |
     $pfx_cert_byte = [System.Convert]::FromBase64String("${{ secrets.WINDOWS_CERTIFICATE_BASE64 }}")
     [IO.File]::WriteAllBytes("certificate.pfx", $pfx_cert_byte)

# 2. 签名 (示例使用 signtool)
- name: Sign Windows Artifacts
  if: matrix.platform == 'windows-latest'
  run: |
     # 这是一个示例，具体取决于你的工具链
     signtool sign /f certificate.pfx /p ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }} /tr http://timestamp.digicert.com /td sha256 /fd sha256 src-tauri/target/release/bundle/nsis/*.exe
```

### 3. 配置 macOS 签名 (需 Apple 开发者账号)

macOS 强制要求签名和公证
(Notarization)。如果不做，用户必须要在“安全性与隐私”中手动允许运行应用。

1. **准备证书**：导出 `Developer ID Application` 证书为 `.p12` 格式（Base64
   编码）。
2. **配置 Secrets**：
   - `APPLE_CERTIFICATE`: Base64 编码的 `.p12` 证书。
   - `APPLE_CERTIFICATE_PASSWORD`: 证书密码。
   - `APPLE_SIGNING_IDENTITY`: 证书名称（例如
     `Developer ID Application: Your Name (TEAMID)`）。
   - `APPLE_API_ISSUER` & `APPLE_API_KEY`: App Store Connect API 信息
     (用于公证)。

3. **修改 `release.yml`**： 将这些变量传递给
   `tauri-action`，它会自动处理签名和公证。

```yaml
- name: Build Desktop App
  uses: tauri-apps/tauri-action@v0
  env:
     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
     # macOS 签名变量
     APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
     APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
     APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
     # ...
```

### 4. 配置 Tauri 更新签名 (Updater Signature - 免费 & 推荐)

这是为了让应用能够安全地检查新版本和自动更新。

#### 1. 生成密钥

在本地开发环境运行：

```bash
npx tauri signer generate -w ~/.tauri/seedclaw.key
```

这将生成一对公钥和私钥。

#### 2. 配置公钥 (应用内)

将生成的公钥内容填入 `src-tauri/tauri.conf.json` 的 `plugins.updater.pubkey`
字段中。

#### 3. 配置私钥 (GitHub Actions)

将私钥内容（文本）和密码分别存入 GitHub Secrets：

- `TAURI_PRIVATE_KEY`
- `TAURI_KEY_PASSWORD` (如果有)

#### 4. 修改 `release.yml`

将这两个变量传递给构建步骤。这样构建出的 `.msi.sig` 和 `.app.tar.gz.sig`
文件就是有效的更新签名文件了。

```yaml
- name: Build Desktop App
  uses: tauri-apps/tauri-action@v0
  env:
     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
     TAURI_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
     TAURI_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
```

这样做之后，每次发版时，GitHub Release 中不仅会有安装包，还会有对应的 `.sig`
签名文件，供老版本的应用进行自动更新校验。

## 三、常见构建问题与排查

### 错误：Resource not accessible by integration

如果你在构建日志中看到类似以下的错误：

> Error: Resource not accessible by integration -
> https://docs.github.com/rest/releases/releases#create-a-release

**原因：** 这意味着 GitHub Actions 的默认 Token (`GITHUB_TOKEN`)
权限不足，无法创建 Release 或上传构建产物。

**解决方法：**

1. **修改仓库权限设置**：
   - 进入 GitHub 仓库页面。
   - 点击顶部导航栏的 **Settings**。
   - 在左侧侧边栏找到 **Actions** -> **General**。
   - 向下滚动到 **Workflow permissions** 部分。
   - 选中 **Read and write permissions** (读写权限)。
   - 勾选 **Allow GitHub Actions to create and approve pull requests**
     (建议开启).
   - 点击 **Save** 保存设置。

2. **重新运行构建**：
   - 权限设置修改后，重新手动触发或推送 Tag 触发一次 Workflow 即可。
